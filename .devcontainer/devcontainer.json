{
	"name": "Debian",
	"build": {
		"dockerfile": "Dockerfile"
	},

	"customizations": {
		"vscode": {
			"settings": {
			},
			"extensions": [
				"ms-vscode.cpptools-extension-pack",
				"mhutchie.git-graph"
			]
		}
	},
	"features": {
		"ghcr.io/devcontainers/features/rust:1": {}
	},

	// "runArgs": [
	// 	"--device=/dev/ttyUSB0", // atmega
	// 	"--device=/dev/video2:/dev/video0", // obs virtual camera or something else
	// ]
}
